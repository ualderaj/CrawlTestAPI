
#Internet connectivity to swapi.dev is required for this application to function.  Application grabs data from one of the 6 films provided by swapi.dev.  Takes the title and the openinc_crawl fields to populate the data it returns.

#two end points
/random_crawl
/random_crawl_reverse

#The following pythons need to installed in addition to python.  See python.org for instructions depending on your OS.
pip install Flask
pip install requests

#To run the application
python main.py

#Than in a browser you can call:
localhost:8080/random_crawl
localhost:8080/random_crawl_reverse
